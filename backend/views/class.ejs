<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Class Announcement: <%= className %></title>
    <style>
      body {
        font-family: Arial, sans-serif;
        line-height: 1.6;
        color: #333;
        max-width: 800px;
        margin: 0 auto;
        padding: 20px;
      }
      h1 {
        color: #2c3e50;
      }
      .important {
        color: #e74c3c;
        font-weight: bold;
      }
      .announcement {
        background-color: #f9f9f9;
        border-left: 4px solid #3498db;
        padding: 15px;
        margin-bottom: 20px;
      }
    </style>
  </head>
  <body>
    <h1>Class Announcement: <%= className %></h1>
    <p>Dear Students of <%= className %>,</p>

    <div class="announcement">
      <h2><%= announcementTitle %></h2>
      <p><%= announcementBody %></p>
    </div>

    <% if (importantDates && importantDates.length > 0) { %>
    <h2>Important Dates:</h2>
    <ul>
      <% importantDates.forEach(function(date) { %>
      <li><strong><%= date.event %>:</strong> <%= date.date %></li>
      <% }); %>
    </ul>
    <% } %> <% if (additionalInfo) { %>
    <h2>Additional Information:</h2>
    <p><%= additionalInfo %></p>
    <% } %>

    <p class="important">Note: <%= importantNote %></p>

    <p>
      If you have any questions or concerns, please don't hesitate to contact
      your class teacher or the administration office.
    </p>

    <footer>
      <p>Best regards,<br /><%= signatory %></p>
    </footer>
  </body>
</html>
